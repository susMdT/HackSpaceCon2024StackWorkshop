Example of Ret Addr Patching  

This code is for demo purposes, -fno-asynchronous-unwind-tables breaks the inline assembly, so it should not work if implemented in PIC.  

Change your SSNs! They're hard coded  